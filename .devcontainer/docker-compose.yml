services:
  y2k38-checker-app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    tty: true
    volumes:
      - ..:/root/y2k38-checker
      - type: bind
        source: ${Y2K38_ANALYSIS_OBJECTS_DIR:-./volumes}
        target: /root/analysis-objects
